# Authentication API

REST API для аутентификации и управления доступом на основе ролей (RBAC).

## 📋 Возможности

- **Аутентификация**: регистрация, логин, refresh токенов, logout
- **JWT токены**: access + refresh с хранением в БД
- **RBAC**: гибкая система прав доступа (CRUD для 3 ресурсов)
- **Валидация**: email через Pydantic `EmailStr`
- **Безопасность**: хеширование паролей bcrypt, очистка просроченных токенов
- **Асинхронность**: полный стек async (FastAPI + asyncpg + SQLAlchemy)

## 🛠 Стек технологий

| Компонент | Технология |
|-----------|------------|
| Framework | FastAPI 0.109 |
| База данных | PostgreSQL 13.3 |
| ORM | SQLAlchemy 2.0 (async) |
| Драйвер БД | asyncpg |
| Аутентификация | PyJWT |
| Хеширование | bcrypt |
| Валидация | Pydantic 2 |
| Контейнеры | Docker, docker-compose |

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd app
pip install -r requirements.txt
```

### 2. Запуск базы данных

```bash
docker-compose up -d
```

### 3. Настройка окружения

Файл `.env` уже настроен по умолчанию:

```env
SECRET_KEY=your_secret_key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=auth_db
```

> ⚠️ **Важно:** замените `SECRET_KEY` на случайную строку перед продакшеном!

### 4. Инициализация базы данных

```bash
python reset_and_populate_test_data.py
```

Создаются тестовые пользователи (пароль: `password123`):

| Email | Роль |
|-------|------|
| `admin@example.com` | admin |
| `ivan@example.com` | user |
| `maria@example.com` | moderator |
| `alexey@example.com` | user |
| `elena@example.com` | manager |

### 5. Запуск сервера

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000

или

python -m uvicorn main:app --reload
```


### 6. Документация API

Откройте в браузере:
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

---

## 📡 API Endpoints

### Authentication (`/authentication`)

<img width="1442" height="336" alt="image" src="https://github.com/user-attachments/assets/df162aa0-3433-49f4-bdca-c2ed59b55423" />

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/register` | Регистрация нового пользователя |
| POST | `/login` | Логин (получение access + refresh токенов) |
| POST | `/refresh` | Обновление токенов по refresh-токену |
| POST | `/logout` | Выход (отзыв refresh-токена) |
| POST | `/cleanup-tokens` | Очистка просроченных refresh-токенов |

### Admin Panel (`/admin`)

<img width="1435" height="386" alt="image" src="https://github.com/user-attachments/assets/d7b299e5-9259-472e-86d1-98855369617a" />

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/users` | Список всех пользователей (требуется `read_all`) |
| GET | `/users/{id}` | Информация о пользователе (требуется `read`) |
| PUT | `/users/{id}/role` | Обновление роли (требуется `update`) |
| PUT | `/users/{id}/activate` | Активировать (требуется `update`) |
| PUT | `/users/{id}/deactivate` | Деактивировать (требуется `update`) |
| DELETE | `/users/{id}` | Удалить (требуется `delete`) |

### User Profile (`/profile`)

<img width="1435" height="218" alt="image" src="https://github.com/user-attachments/assets/2f971111-daf6-4842-8469-9a1989c621d6" />

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/` | Профиль текущего пользователя |
| PUT | `/delete` | Деактивация аккаунта (soft delete) |
| PUT | `/update/{parameter}` | Обновление поля профиля |

### Permissions (`/permissions`)

<img width="1435" height="331" alt="image" src="https://github.com/user-attachments/assets/c91b652c-684d-46af-951b-293ef69cdadf" />

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/` | Все права доступа (требуется `read_all`) |
| POST | `/{role_name}` | Создать права для роли (требуется `create`) |
| GET | `/{role_name}` | Права для роли (требуется `read`) |
| PUT | `/{role_name}` | Обновить права (требуется `update`) |
| DELETE | `/{role_name}` | Удалить права (требуется `delete`) |

### Business Elements (`/business-elements`)

<img width="1436" height="382" alt="image" src="https://github.com/user-attachments/assets/c1faf0ba-c538-4dd1-bf36-672a98a060c5" />

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/` | Все бизнес-элементы (требуется `read_all`) |
| GET | `/{name}` | Элемент по имени (требуется `read`) |
| POST | `/` | Создать элемент (требуется `create`) |
| PUT | `/{name}` | Обновить элемент (требуется `update`) |
| DELETE | `/{name}` | Удалить элемент (требуется `delete`) |
| GET | `/{name}/` | Mock-view с проверкой роли |

---

## 🔐 Система прав доступа

### Ресурсы и действия

| Ресурс | create | read | read_all | update | delete |
|--------|--------|------|----------|--------|--------|
| `users` | ✓ | ✓ | ✓ | ✓ | ✓ |
| `permissions` | ✓ | ✓ | ✓ | ✓ | ✓ |
| `business_elements` | ✓ | ✓ | ✓ | ✓ | ✓ |

### Роли по умолчанию

| Роль | Описание |
|------|----------|
| `admin` | Полный доступ ко всем ресурсам |
| `moderator` | Чтение/обновление пользователей и элементов |
| `manager` | Чтение всех пользователей, управление элементами |
| `user` | Базовый доступ (чтение своих данных) |

---

## 📁 Структура проекта

```
auth_api/
├── app/
│   ├── main.py                     # Точка входа FastAPI
│   ├── config.py                   # Настройки (pydantic-settings)
│   ├── .env                        # Переменные окружения
│   ├── docker-compose.yml          # Docker-конфигурация
│   ├── requirements.txt            # Python-зависимости
│   ├── reset_and_populate_test_data.py
│   ├── database/
│   │   ├── database.py             # DB engine, сессии
│   │   └── models_db.py            # SQLAlchemy модели
│   ├── routers/
│   │   ├── auth.py                 # Аутентификация
│   │   ├── admin.py                # Админ-панель
│   │   ├── user.py                 # Профиль пользователя
│   │   ├── permission.py           # Управление правами
│   │   └── business_elements.py    # Бизнес-элементы
│   └── tools/
│       ├── auth_func.py            # JWT, проверка прав
│       ├── hash.py                 # Хеширование bcrypt
│       └── schemas.py              # Pydantic схемы
└── README.md
```

---

## 🔧 Примеры запросов

### Регистрация

```bash
curl -X POST http://localhost:8000/authentication/register \
  -H "Content-Type: application/json" \
  -d '{
    "first_name": "John",
    "last_name": "Doe",
    "patronymic": "Ivanovich",
    "email": "john@example.com",
    "password": "password123",
    "password_confirm": "password123"
  }'
```

### Логин

```bash
curl -X POST http://localhost:8000/authentication/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "password123"
  }'
```

### Запрос с токеном

```bash
curl -X GET http://localhost:8000/profile/ \
  -H "Authorization: Bearer <access_token>"
```

---

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `SECRET_KEY` | `your_secret_key` | Ключ для JWT |
| `ALGORITHM` | `HS256` | Алгоритм шифрования JWT |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | `15` | Время жизни access-токена |
| `REFRESH_TOKEN_EXPIRE_DAYS` | `7` | Время жизни refresh-токена |
| `DB_USER` | `postgres` | Пользователь БД |
| `DB_PASSWORD` | `postgres` | Пароль БД |
| `DB_HOST` | `localhost` | Хост БД |
| `DB_PORT` | `5432` | Порт БД |
| `DB_NAME` | `auth_db` | Имя базы данных |


---

## 📝 Лицензия

MIT
